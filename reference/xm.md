# Simulated data for examples xm \<- sim.ind.simple(mvec = c(0.5))

Simulated data for examples xm \<- sim.ind.simple(mvec = c(0.5))

## Usage

``` r
xm
```

## Format

A data frame with 2 columns and 50000 rows
